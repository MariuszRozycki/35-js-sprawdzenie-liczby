function number(n) {
  if (n >= 100 && n <= 200) {
    console.log(`Liczba znajduje sie w przedziale 100 - 200`);
  } else {
    console.log(`Liczba nie znajduje sie w przedziale 100 - 200`);
  }
}

number(108);